sensitive:
  enabled: true

  # 文本模式扫描：全局关闭，SQL 独立开启（向后兼容默认行为）
  # maskEnhanced() 测试依赖 sql: true
  textPattern:
    enabled: false
    sql: true
    patterns:
      - phone
      - idcard
      - bankcard
